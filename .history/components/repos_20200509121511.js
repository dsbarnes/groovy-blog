import frontEnd from '../siteData/frontEnd.js'
import frontEnd from '../siteData/backEnd.js'
import frontEnd from '../siteData/fullStack.js'

function repos(arr) {

    const pTag = document.createElement('p')
    pTag.classList.add('fade-in')
    pTag.innerHTML = 

    for (let element of arr)
        console.log(element)
}

export default repos